#pragma once
// -- global variables -- //
const int SCREEN_MULTIPLIER = 8;
const int L_WIDTH = 8 * 12;
const int L_HEIGHT = 8 * 8;
const int SCREEN_WIDTH = L_WIDTH * SCREEN_MULTIPLIER;
const int SCREEN_HEIGHT = L_HEIGHT * SCREEN_MULTIPLIER;
